<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>User Edit</title>
</head>
<body>
    <h1>수정하기</h1>
    <form action="/update/<%= user[0].id %>?spec=<%= user[0].ovspec %>" method="POST">
         제품명 <input type="text" name="sname" value="<%= user[0].ovname %>"><br>
        사이즈<input type="text" name="ssize" value="<%= user[0].ovsize %>"><br>
        옷 종류 <input type="text" name="sspec" value="<%= user[0].ovspec %>"><br>
        이미지 <input type="file" name="simage" value="<%= user[0].ovimage %>" required><br>
        옷장 종류 <input type="text" name="scloset_spec" value="<%= user[0].ovcloset_spec %>"><br>

        <button type="submit">수정완료</button>
    </form>    
</body>
</html>